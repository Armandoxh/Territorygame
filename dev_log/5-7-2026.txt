SESSION: 2026-05-07
=====================

Big mechanical shift session. Game went from "free-for-all expansion
with light diplomacy" to "Civ-style political model with peace as
default + commander-tree depth that actually applies in play".

Order is roughly chronological. Each entry is WHAT changed + WHY.

---

[a133e01] gameplay: manual click = 100% focus; vassals aggressive on
neutrals

WHAT: Restructured _expand candidate selection. Three modes per
frontier tile: (1) MANUAL — pool restricted to candidates inside a
manual-target region, no fallback; (2) VASSAL AUTONOMOUS — vassal
target first, else any adjacent neutral, with +50% rate boost when
targeting neutrals; (3) IDLE — non-vassal tile with no manual order
does nothing. Peace-state filter folded into candidate list.

WHY: User reported clicking a region "feels like spread 3× in all
directions" instead of focused push. Root cause: focus multiplier
applied to every leader-driven frontier tile regardless of whether it
could actually contribute to the front. Tiles with no target-region
neighbor fell through Tier 2/3 fallback and still got the boost,
attacking arbitrary unclaimed land at 3×. Fix makes "click = general's
order" literally true — only contributing tiles push, others sit out.

Also user wanted vassals to "expand autonomously and aggressively into
contacting neutrals" early game. The +50% boost in autonomous-on-
neutral mode addresses that.

---

[f5a6e5f] fix: diplomacy button vanished — bad position override on
#diplo-btn

WHAT: Removed `#diplo-btn { position: relative; }` rule.

WHY: Two commits earlier, that rule was added to anchor the new
notification badge. But .icon-btn already has position:absolute, and
overriding it with position:relative collapsed the button into normal
document flow — invisible behind the canvas. Absolute children resolve
against the nearest positioned ancestor, which the absolute parent
already provided.

---

[5a4fac7] ux: diplomacy panel defaults to active relationships only

WHAT: Hide "OTHER NATIONS" section by default; show only YOUR ALLIES
and AT WAR. "Show N Other Nations" toggle at the bottom expands to
the full list.

WHY: User wanted a panel that surfaces what they actually need to
manage. Listing 50 peaceful AIs was noise. Long-press enemy land
already proposes alliance, so propose-from-panel is rare; the
collapsed section preserves the option without making it the default.

---

[09a84b8] ux: diplomacy panel sections + war-invite validation + badge

WHAT: Three things. (1) Diplomacy panel grouped into sections:
ALLIES (green border), AT WAR (red border), OTHERS — instead of one
flat list. (2) War-invite popup drops stale invites if inviter is no
longer an ally. breakAlliance also clears any pending invites between
the broken pair. (3) Notification badge on the ⚑ button showing count
of pending war invites.

WHY: User reported (a) panel was overwhelming "AI 1, AI 2... AI 50"
with no order, (b) AI was sending war invites despite not being an
ally — turned out to be stale invites from broken alliances, and (c)
needed a way to know there's something to look at without opening
the panel.

---

[9b53f29] diplomacy overhaul: peace by default, permanent alliances,
war system

WHAT: Big one.
  • Default state for any pair = PEACE. tryCapture refuses to flip
    a defender's tile unless at war (or neutral). Same gate added
    to ship fire / submarine missiles / warship landings.
  • Aggression auto-declares war: tap an enemy region, drop a bomb
    on enemy land, or attack via ship. Toast shows cause→effect.
  • Alliances now permanent (Infinity expiry) instead of 60s.
    Forming an alliance ends any active war between the pair.
  • declareWar bandwagons the target's allies into the war on
    target's side (one level deep).
  • New diplomacy buttons: Declare War / Sue for Peace, Make Them
    Fight (coerce ally to declare war on your enemies).
  • coerceAllyToWar accept rule keys off ally vs target strength
    ratio; Embassy decree loosens the threshold.
  • War-invite system: AI declares war → if it has human ally,
    pendingWarInvites queue gets an entry. Red modal at top of
    screen with [Join War] / [Decline].
  • Ally indicator on map: capital crowns now color-coded —
    green double-halo for allies, red ring for enemies, white pulse
    for neutral.
  • _aiConsiderWar runs every ~10s per AI. Peaceful AIs prefer
    expanding into neutral. Only declare war when no neutrals
    adjacent AND 1.3× strength advantage.

WHY: User wanted a Civ-style political model: nations don't auto-
fight, aggression is a deliberate choice with diplomatic cost,
allies stick around long enough to matter, and "you can coerce
allies + AI invites you to wars". Game previously had alliances
existing but expiring after 60s and constant chaotic warfare with
no peace state. The map gave no visual indicator of who was allied.

---

[4818188] fix: AI players actually buy decrees now (full audit pass)

WHAT: New _aiBuyDecrees runs every ~10s per AI. Picks highest-priority
affordable decree from a mastery-tilted list (naval AIs lean into
admiralty/embassy/cartel, ground into iron-doctrine/bunkers/veterans,
air into air-supremacy/forced-march). Soft-cap 8 stacks per decree.

Audited every decree's effect path; documented in commit message.

WHY: User asked to verify all commander perks apply. Audit showed
every effect was wired — but buyDecree was only called from the human
HUD. AIs never invested in any decree, so their treasury just stockpiled
unused while the human stacked +30 forced-march unopposed. Asymmetric
late-game advantage hardcoded into the engine.

---

[b974df4] balance + perks: trade rebalanced, two new branches, four
new decrees

WHAT:
  • Trade per-region rate dropped 5g/s → 1.5g/s.
  • Diminishing returns on alliance count: 1st 100% / 2nd 70% /
    3rd 50% / 4th 35% / 5+ 25%.
  • Hard cap removed — diminishing returns naturally bound it.
  • New NAVAL branch: Admiralty (T1, +20% ship speed/–20% cost),
    Privateer (T2, coming soon).
  • New DIPLOMACY branch: Embassy (T1, AI more lenient), Cartel
    (T2, +20% trade income), Cold War (T3, coming soon).
  • Existing branches filled: Master Builder (econ), Reinforced
    Bunkers (def), Veterans (mil), Forced Labor (esp, coming soon).

WHY: User reported trade income compounded with commander boosts felt
"broken" — three alliances printing 120g/s. Per-region rate was 5g/s
with no diminishing, scaled linearly with allies. Lower base + dim
returns keeps trade meaningful (one good alliance = ~10g/s) without
snowballing. The two new branches address two earlier complaints —
"naval class doesn't get much love" and "trade alliances are central
now but have no decree tree".

---

[1bcb8f8] ux: long-press enemy land for one-tap trade alliance

WHAT: Long-press on enemy territory → propose alliance + open trade
route in one action. Long-press on own/neutral → still build sheet.

WHY: User wanted "set up a trade alliance" to be a direct gesture
instead of navigating through the diplomacy panel. Mobile-first
muscle memory. Toast shows result + per-second income.

---

[e1d2005] fix: production decree applies empire-wide; buildings drop
on flip

WHAT: (1) Production decree now multiplies BOTH per-tile income AND
settlement flat income; previously only per-tile. (2) tryCapture
+ morale collapse + ship landings all destroyed buildings via
explicit calls; pulled it into _claim itself so every tile-flip
path automatically destroys buildings.

WHY: (1) User asked which decrees might have similar gaps to
forced-march; audit found settlement flat-gold skipped production
multiplier, so a settlement-heavy economy got half the boost.
(2) User reported turrets/settlements lingering on enemy land after
capture in some paths. The new morale-collapse defection routine
called _claim directly without destroying buildings — zombie turrets
on enemy land buffing the wrong owner's defense map.

---

[9ab4146] gameplay: decrees stack with manual play + siege actually
cracks

WHAT:
  • _expansionBoostFor (forced-march) now applied to BOTH manual
    and vassal-driven expansion paths.
  • Vassal expansion gets a treasury fallback when _vassalGold[r]
    runs dry.
  • New morale-collapse defection: regions pinned at floor morale
    (0.20) for 30s under siege start losing one tile every 4 ticks
    to the strongest besieger. Picks frontline tiles.

WHY: User said "decree investment feels invisible during play" + "no
matter what I build on my vassals they never overtake nearby land"
+ "late game feels like a stalemate". Diagnosis: forced-march was
only on vassal-driven path, vassal pools drained from builds and
stalled, and siege fatigue only drained morale without ever flipping
tiles. All three fixed in one coherent change.

---

[d9489e2] gameplay: focus multiplier — concentrated taps push faster

WHAT: New MANUAL_FOCUS_BOOST = 3.0. Single manual target rolls at
3× baseline; N targets divides the boost by N. STOP fab text now
shows current multiplier.

WHY: User wanted 1-target taps to feel like a focused push. Same
session this introduced a side effect (spread-3×-everywhere) that
got fixed properly in [a133e01].

---

[f3ee1aa] fix: manual taps override vassal autonomy (the real
expansion stall)

WHAT: When the leader has a manual target and a frontier tile's
region is adjacent to (or matches) that target, the leader's command
overrides the vassal's autonomous target for that tile. Pays from
p.gold + treasury fallback.

WHY: Bomb the enemy → click the cleared region → units don't push.
Cause: design comment in _expand said "Manual override does NOT
apply here", so vassal-tile autonomous target hijacked the player's
manual click. Compounded by vassal pool starvation.

---

[6b9f2d5] phase 3: external trade routes between allies

WHAT: Diplomatic trade routes. Hard-gated on alliance — auto-cancel
when alliance ends. Both sides earn flow into treasury per tick.
HUD: Open/End Trade Route button, ROUTE tag with live income.
Render: dashed cyan line + animated dots between players' largest-
region centroids.

WHY: User confirmed Phase 3 of the OVERHAUL.md plan. Trade as a
diplomatic carrot beyond the existing one-shot exchanges.

---

[264177d] fix: bombed-out regions can be re-targeted instantly (no
false cooldown)

WHAT: REGION_LOSS_COOLDOWN now only enforces if an enemy currently
holds the region. Bomb-wiped no-dominant case drops the cooldown.

WHY: Tap a region you just got bombed in, troops don't advance.
Cause: cooldown fired whenever you lost dominance regardless of who
took it; bomb wipes drop you below 50% but region usually goes to
no-dominant, not enemy-dominant. False positive — there's no yo-yo
to prevent if no enemy actually conquered the region.

---

[0f439fb] perf: fog-cull buildings + O(1) building-version signature

WHAT: Buildings + coverage rings inside fog-hidden regions are
skipped during draw. New buildingsVersion counter bumped on every
build/upgrade/destroy/consolidate; replaces the O(buildings)
levelSum scan in the redraw signature.

WHY: User reported late-game framerate drops. Two costs identified:
(1) painting every distant building every redraw, (2) signature
scan running per-frame even when sitting still. Fog-cull addresses
the user's "tree depth 2" intuition naturally.

---

KEY INVARIANTS LEARNED THIS SESSION
====================================

1. ALL tile-flip paths must destroy buildings. _claim does this
   centrally now — don't call territory.claim directly.
2. Decree effects must apply on BOTH manual and vassal paths. Helpers
   like _expansionBoostFor / _productionMult are called multiple
   places; always grep before assuming a new decree "works".
3. AI must invest in decrees (_aiBuyDecrees). Without that, any new
   decree is a one-sided human-only buff.
4. Peace is the default. tryCapture / ship fire / submarine missiles
   all gated on areAtWar(). Filter candidate lists upfront in _expand
   so we don't waste rolls.
5. Alliances are permanent (Infinity expiry) unless explicitly time-
   limited. allianceExpireAt() returning Number.POSITIVE_INFINITY is
   normal; HUD displays "ALLY" without countdown in that case.
6. Manual target click = "general's order" — only frontier tiles
   that can directly contribute push. Don't add Tier 2/3 fallback
   that bypasses the focus.
7. Tile-flip paths route through _claim, but pendingWarInvites and
   trade routes can become stale if alliances end — clean them up
   in breakAlliance.

FILES MOST TOUCHED THIS SESSION
================================
shared/src/game.ts        — core engine (peace/war, _expand, AI logic)
shared/src/decrees.ts     — new branches + decree definitions
shared/src/types.ts       — new event types, ExternalTradeRoute
shared/src/config.ts      — MANUAL_FOCUS_BOOST and rebalance constants
client/src/ui/HUD.ts      — diplomacy panel restructure, war-invite UI
client/src/main.ts        — long-press routing for trade alliances
client/src/render/OverlayLayer.ts — capital halos by relationship
client/src/render/TradeLayer.ts   — external route rendering
client/src/styles.css     — diplo sections, war-invite, badge styles
client/index.html         — war-invite popup, badge span, naval/diplo tabs

---

[pending] gameplay: AC-130 buffed; new Stealth Bomber (1000g carpet)

WHAT:
  AC-130 buff:
    - Orbit duration 100 → 150 ticks (10s → 15s on station)
    - Strafe interval 14 → 9 ticks (~1.4s → ~0.9s, more passes)
    - Per-strafe blast radius 2 → 3
    - Per-strafe troop drain 25 → 60 (×2.4)
    - Per-strafe ship damage 15 → 35
    - Orbit-zone radius (BOMB_RADII['ac130']) 4 → 6 (wider area covered)
    Net: ~16 strafes per orbit covering a 12-tile-diameter footprint —
    cumulative wipe now solidly out-clears a Large bomb, justifying 350g.

  Stealth bomber:
    - New BombType 'stealth', cost 1000g, cooldown 60s
    - Plane speed 0.55 (fastest of any bomber)
    - On arrival, lays a CARPET of 7 bomblets along the flight axis
      from origin → dest, spaced 5 tiles apart, each radius 4
    - Net coverage ≈ 30 tiles long × 8 tiles wide
    - Per-bomblet 40 troop drain on owned tiles, 80 ship damage
    - Bypasses AA entirely — no rolledAA check applied
    - HUD: new bomb-sheet button with chevron icon, hotkey X
    - Plane visual: dark angular flying-wing with owner-tint stripe
    - New Plane.originX/originY field tracks spawn point so the carpet
      orients along the actual flight axis

WHY:
  User said AC-130 felt underwhelming for its 350g cost. Per-strafe was
  too small (radius 2, 25 troops) and orbit was short, so total per-cast
  damage trailed Large bomb at 240g. Boosted across all axes.
  
  User asked for a Stealth Bomber: high-cost premium aircraft that does
  carpet bombing. Carpet implemented as a strip of overlapping bomblets
  along the flight axis (looks/feels like a bombing run). AA bypass is
  what justifies the 1000g — defenders can't stop it once launched, so
  it's the answer to a heavily-fortified AA umbrella.

---

[pending] ux: sticky placement modes — drop multiple without re-arming

WHAT: tryPlaceAt / tryBombAt / tryBuildShipAt no longer clear their
mode after a successful action. Player can drop 10 settlements / 5
turrets / chain bombs / build a fleet without re-pressing the hotkey
or re-opening the sheet between each one. Esc still exits cleanly,
re-pressing the same hotkey toggles off, opening a different mode
(build vs bomb vs ship) clears the others.

WHY: User wanted "drop 10 settlements at once without pressing s
between each drop" — was annoying to re-arm every time during early
game build-up.

---

[pending] gameplay: only the leader declares war; vassals never auto-aggress

WHAT:
  • _vassalRetarget skips any adjacent region whose dominant owner
    is a peace-state non-allied player. Vassals only choose neutrals
    or at-war enemy regions as their target.
  • _vassalMaybeBomb early-exit if the target region's dominant owner
    is non-war non-self (i.e. a peace neighbor). Without this guard,
    a vassal-driven bomb on a peaceful neighbor's land would auto-
    declare war via dropBomb's aggression check.

WHY: User reported vassals were independently dragging them into
wars by bombing neighbors. Foreign policy is a leader-only decision —
vassals should expand into neutrals + fight wars the leader has
sanctioned, but never start one on their own.

---

[pending] gameplay: AA tiers + Defense Port + Artillery + ground perks

Late-game air dominance was a real problem — mass-bomb spam was the
dominant strategy and most AIs never built AA (it was air-mastery
gated). Ground play also lacked active push abilities while air/naval
had bombs/ships to tilt fights.

WHAT:
  AA overhaul:
    • Per-level hit rates: L1 50%, L2 60%, L3 75%, L4+ (consolidated)
      85%. _aaHitChanceFor helper.
    • AA included in _tryConsolidate so 5 same-tier cluster auto-
      upgrades (Bronze/Silver/Diamond), like settlements/turrets.
    • AA no longer gated on air-mastery — every player can build it.
      Defense should be universal; mastery still differentiates
      offense (airstrips/bombs).
    • aaCount per region cap added (1 per 50 tiles).

  Defense Port (NEW building):
    • New BuildingType 'port'. Cost 180g. Coastal-only (rejected if
      no adjacent water tile).
    • _portsTick scans every tick: each port rolls one shot per
      enemy ship in PORT_RADIUS (8). Hit rate uses same tier scale
      as AA (L1 50% → L4+ 85%). Damage 25 base, ×1.25 / ×1.50 / ×2
      by tier.
    • Same auto-consolidation as AA.
    • _anyEnemyHasShips drives AI port-build trigger (mirror of AA).
    • Build-sheet button hotkey P, anchor icon, blue-tinted ring.

  Artillery (NEW building):
    • New BuildingType 'artillery'. Cost 140g. Long range (turret
      base radius +3) but NO defense bonus — only retaliation.
      3× turret retaliation per level, so a bronze artillery covers
      a wide area and shreds attackers passing through.
    • Same auto-consolidation as turret.
    • Hotkey R. Cannon icon, orange-tinted ring.

  Ground active abilities (NEW):
    • Blitzkrieg (cost 900, 30s cooldown, instant): each region you
      dominate fires a BFS burst from its frontier, claiming up to
      10 tiles deep. Ignores defense. Only takes neutral or at-war
      tiles (no auto-declare). Per-region cap so the burst feels
      like a deep push from each vassal rather than a global cone.
    • Tank Rush (cost 1100, 80s cooldown, 15s duration): empire-
      wide ×2.0 attacker combat-power multiplier. Stacks
      multiplicatively with Veterans / Rally / focus.
    • Both wired into activateAbility with effects in _fireBlitzkrieg
      / _tankRushMult. Surface in the existing commander abilities
      panel automatically.

WHY: User reported (a) air-spam dominated late game with no real
counter, (b) AI rarely built AA so air players had free reign,
(c) ground mastery felt purely passive while air/naval had active
push tools. Three new structures + two new active abilities give
ground a comparable late-game power curve and force air players
to commit to bigger payloads (or stealth) to break through.

User asked for tank "steamroll" — implemented as Tank Rush ability
since per-tile units would require a lot more engine work.
Blitzkrieg is the per-vassal burst as requested. Both are listed
in the abilities panel and pay from treasury.

---

[pending] gameplay: ground perks moved from commander to a Barracks
building (parallel to airstrip → bombs)

WHAT:
  • Removed Blitzkrieg + Tank-Rush from ABILITIES (commander panel).
  • Removed static 'artillery' building type entirely (its retaliation
    effect went away).
  • New 'barracks' BuildingType (cost 140g, no special effect on its
    own — it's an enabler, like an airstrip).
  • New GroundOpType: 'blitzkrieg' | 'artillery' | 'tanks'.
  • New tryGroundOp(type, x, y, ownerId) engine API. Mirrors dropBomb:
    finds the soonest-ready Barracks for that op, charges treasury,
    sets per-op cooldown on that Barracks, applies effect.
  • Per-Barracks per-op cooldowns stored on Building.opCooldowns
    (Partial<Record<GroundOpType, number>>) so build more barracks =
    more parallel deployments, but same barracks can't spam the same
    op back-to-back.
  • Effects:
      blitzkrieg (300g, 30s): every frontier tile within 8 tiles of
        the target launches a 10-tile-deep BFS claim toward target,
        ignoring defense. Only takes neutral / at-war tiles.
      artillery (250g, 20s): single shell at target. Drains 12 troops
        per hit tile from each enemy owner (capped 800), 60hp ship
        damage in r5. No claim wipe — pure attrition.
      tanks (400g, 60s): claim a 8-tile line from your nearest frontier
        toward target, ignoring defense. +50% combat power buff for
        the next 8s after launch (sets activeBuffs['tank-rush']).
  • Build sheet: Artillery button replaced with Barracks (hotkey G).
  • New "barracks-sheet" mirroring the bomb sheet, opened by the new
    ⚔ FAB button (only shows once you have a Barracks).
  • Per-barracks cooldown reads in HUD ground-op buttons so they
    grey out when reloading.
  • OverlayLayer: barracks icon (bunkered hut + flagpole). No coverage
    ring (it's an enabler, not an aura piece).
  • Aggression on a peace neighbor's tile auto-declares war via
    declareWar (same gate as bombs).

WHY: User wanted ground troops to have a parallel push system to
airstrips/bombs and ships, with deployments fired from a building
rather than commander treasury. Cleaner separation: commander panel
holds passive decrees + tactical buffs (Rally, Reinforcements);
Barracks holds offensive ground deployments. Encourages investing
in a Barracks per region for parallel firing capability.

---

[pending] gameplay: ships gated behind Defense Port (parallel to
airstrip → bombs and Barracks → ground ops)

WHAT:
  • buildShip now requires the player to own at least one 'port'
    building. Replaces the old `isUnlocked(ownerId, 'ships')` mastery
    gate. Naval mastery still buffs ship cap and reload — it just
    doesn't gatekeep construction anymore.
  • New 'no-port' ShipBuildError → toast: "Build a Defense Port first".
  • Fleet FAB hides entirely until the player has a Port (matches
    bomb-fab → airstrip and barracks-fab → barracks pattern).
  • AI ship-build path (`_aiMaybeBuildShip` trigger) gated on
    hasPort rather than mastery unlock.
  • Naval-mastery AIs now build a Port proactively (not just reactive
    to enemy ships) since they need it to build ships at all.
  • Port build-sheet desc updated: "Coastal · enables ship build ·
    sinks enemy ships in r8".

WHY: User wanted ships behind ports the same way bombs are behind
airstrips and ground ops are behind barracks. Single unified pattern
across all three offensive systems: enabler-building → fab →
deployment sheet. Also gives the Defense Port an offensive value
alongside its defensive role — invest once, get both.

---

[pending] gameplay: artillery rolls + barrages; tank push is a V-cone

WHAT:
  • Artillery Strike no longer single-shells at target. Spawns an
    ArtilleryUnit (new entity) at the closest Barracks. The unit rolls
    toward the player's nearest tile to target at 0.35 tiles/tick, then
    sets up and barrages — 7 small radius-2 shells over 8s, scattered
    in r4 of the target zone. AC-130-style cadence, ground-launched.
    Each shell drains 30 troops/tile per enemy + 25hp ship damage.
    OverlayLayer renders the rolling tank-with-cannon silhouette plus
    a muzzle-flash flicker during the firing phase.
  • Tank Push no longer a 1-tile straight line. Now a V-cone (60° wedge)
    of DEPTH (8) tiles outward from the closest frontier tile to target.
    Sweeps every tile inside the cone, claims eligible ones (neutral or
    at-war non-allied). Capped at 32 claims to prevent runaway. Same
    +50% combat-power buff for 8s after.
  • New _artilleryTick processes phase transitions; new _fireArtilleryShell
    helper reuses 'bomb' event for explosion VFX.

WHY: User feedback — single-shot artillery felt anonymous, and a 1-tile
line tank push didn't feel like a steamroll. Both are now visibly
expressive: artillery has a deployment story (roll → fire → leave),
tanks paint a wedge of conquered territory you can see at a glance.

---

[pending] gameplay: resource layer (Phase 1 of "depth" overhaul)

Adding strategic resources (food/wood/stone/oil/gems) as a parallel
economy alongside gold. Phase 1 lands the layer; Phases 2+3 add
cost/upkeep rework and trade routes. Shipped in this commit:

  • New ResourceKind type + ResourceBag helper.
  • Per-region resource assignment at game start. Distribution:
    food 30%, wood 25%, stone 22%, oil 15%, gems 8%. Deterministic
    per-region (hash on regionId).
  • Player.resources inventory (5 floats, 0-init).
  • _generateResources runs each tick: every owned tile in a region
    contributes its region's resource at a per-resource rate. Gems
    intentionally an order of magnitude slower so they stay scarce.
  • Vassal target picker: regions with a resource the leader is short
    on (< 100 inventory) get +600 score. Vassals naturally roll toward
    strategically valuable land.
  • Top-bar resource strip showing all 5 inventories with per-resource
    color coding (food green, wood olive, stone grey, oil amber, gems
    blue-white).
  • OverlayLayer: resource emoji per region centroid (🌾🌲⛰🛢💎),
    visible at the same zoom the region names show.

NEXT (per user spec):
  • Phase 2: building costs in resources + per-tick resource upkeep.
  • Phase 3: indefinite resource trades between players.

---

[pending] gameplay: Phase 3 — buildings cost resources + upkeep

WHAT:
  • New BUILDING_RESOURCE_COSTS table per BuildingType:
      settlement: 25 food + 15 wood
      turret:     30 stone + 15 wood
      airstrip:   40 stone + 25 oil + 5 gems
      aa:         25 stone + 15 oil
      port:       35 wood + 25 stone
      barracks:   30 wood + 20 stone
    Charged on tryBuild in addition to gold. Empire-wide pool (no
    per-vassal resource pool yet).
  • New BUILDING_UPKEEP table — per-tick resource drain per owned
    building. Settlement 0.002 food/tick, airstrip 0.005 oil/tick,
    AA 0.0015 stone + 0.001 oil/tick, etc.
  • _payBuildingUpkeep runs each tick. If owner can pay all components,
    drain. If not → mark Building.starvingSinceTick. After
    STARVING_DECAY_TICKS (30s) of continuous starvation the building
    self-destructs via _destroyBuildingsAt.
  • New BuildError 'resources' returned from tryBuild when any
    component is short. HUD toasts "Need more resources (see top bar)".
  • Build sheet now shows resource costs alongside gold cost
    ("60g 25🌾 15🌲"). Buttons disable when ANY component (gold OR
    resources) is unaffordable.

WHY: Phase 3 of the resource overhaul. User wanted buildings to cost
"a combination of resources" with upkeep. Forces the player to think
strategically about which regions to capture (your ability to build
turrets depends on owning stone-producing regions; airstrips need oil;
etc.). Sustained shortages start eating your infrastructure — late-
game neglect actually has consequences.

NEXT (Phase 4): trade overhaul — resource-specific trades, indefinite
flow until cancelled, AI-aware ("I need stone, propose stone-for-oil
trade"), with diplomatic pressure when needs aren't met.

---

[pending] graphics: biomes match the region's resource

WHAT:
  • Per-tile resource id now encoded into territory texture B channel.
    game.resourceByTile pre-computes id per tile from regionResources;
    flushDirty + _fillFromGrid write it. Static after init.
  • Shader land branch rewritten: biome color is picked from resource
    id (food = golden plains, wood = deep forest, stone = cool grey,
    oil = dark earthy brown, gems = silvery white). No more arbitrary
    elevation/moisture coloring — the map's color now teaches the
    player what each region produces.
  • Each resource has a distinct PROCEDURAL pattern overlay:
      food  → wheat-row striping + crop divisions
      wood  → tree-blob noise clusters with canopy highlights
      stone → rocky chunks + cracks
      oil   → viscous splotches with iridescent slick highlights
      gems  → rare animated sparkle points on silver base
    All cheap fragment-level procedurals — no asset pipeline.
  • Bilinear smoothing of owner tints still wraps over the resource
    biome so empire colors blend smoothly across resource regions.

WHY: User pointed out that Phase 2 polish was incomplete — the colors
were arbitrary biome bands not tied to resources. Now the map's
visual layer literally communicates the strategic layer. You can spot
oil regions (dark slick), wood (tree clusters), gems (sparkle) at a
glance from any zoom — vital with the new resource-cost economy.

---

[pending] gameplay: Phase 4 — resource-aware indefinite trades + AI

Closes the resource overhaul. Players (and the AI) can now negotiate
ongoing resource-for-resource exchanges that flow per tick until
cancelled by either side.

ENGINE:
  • New ResourceTrade struct: { a, b, aGives, aGivesPerSec, bGives,
    bGivesPerSec, startedTick }. Stored in game.resourceTrades.
  • proposeResourceTrade(from, to, aGives, aRate, bGives, bRate):
    if `to` is human → queues a ResourceTradeOffer in
    pendingResourceOffers (HUD shows a popup like war invites);
    if `to` is AI → routes through _aiAcceptsResourceTrade.
  • _aiAcceptsResourceTrade: accepts if the resource RECEIVED is one
    we're short on (< 250) AND we have enough of what we GIVE
    (>100 + 30s of payments). Embassy decree loosens the buffer.
  • _flowResourceTrades runs each tick: pays both sides their
    perSec / SIM_HZ. If either side can't pay, the trade silently
    pauses that tick (no decay, no destruction) and resumes next
    tick when the resource comes back.
  • _aiProposeResourceTrade runs every ~20s per AI, offset per
    player id. AI finds its shortest resource (under 150) + biggest
    surplus (over 200), picks a peer with > 100 of the needed
    resource, proposes 5/sec each direction.
  • cancelResourceTrade ends a trade — either side can call. New
    Game.paused flag honored by tick() so the prompt can freeze the
    sim while composing.

UI:
  • New trade-prompt modal: title shows target name; Give/Receive
    sections each with a kind dropdown + step buttons + per-second
    amount + stock readout ("you have 234 food", "AI 1 has 88 stone").
    Send Offer / Cancel actions. Sets Game.paused while open.
  • Long-press enemy land now opens the trade prompt instead of
    the old alliance shortcut. Long-pressing an EXISTING ally still
    routes to quickProposeTradeAlliance for the gold-flow trade route.
  • New resource-offer popup (resource-offer element) for incoming
    AI offers — same shape as war-invite popup: shows
    "AI X offers 5 stone/s for 5 oil/s" with Accept / Decline.
  • Per-diplomacy-row: new "Resource Trade" / "Cancel Trade" buttons
    + an active-trade summary line ("↻ 5 food/s → ⇄ ← 5 stone/s")
    when a trade is live.
  • Diplomacy badge on the ⚑ button now counts pending resource
    offers in addition to war invites.

WHY: Resources alone with no exchange mechanism would be too punishing
— if your map has no oil regions, you can't build airstrips ever.
Trades let you specialize: raid the gem mountains and trade gems for
the things your terrain doesn't naturally produce. AI awareness means
the simulation reacts to shortages instead of just stockpiling.

---

[pending] ux: trade-status block + AI desperation-scaled offers

WHAT:
  • New TRADE STATUS block pinned at top of diplomacy panel. Three
    sub-sections: NET FLOW row (per-resource +/- chip across ALL
    active trades, color-coded green/red), INCOMING OFFERS list
    (every pending offer ranked by deal score with Accept/Decline
    inline + GREAT / FAIR / WEAK rating tag), ACTIVE TRADES list
    (every live trade with per-second in/out and Cancel button).
  • Score formula: (received_per_sec / sent_per_sec) × shortage
    weight (0.5×–2× based on stock of received resource).
  • _aiProposeResourceTrade now scales offer asymmetry by
    desperation: an AI dangerously short on the resource it needs
    proposes "12 wood/s for 1.7 oil/s" instead of an even 5/5.
    Recipients see clearly attractive offers when the seller is
    truly hurting.

WHY: User reported two problems with the Phase-4 trade flow.
(1) "very difficult at the moment to know what active trade deals
I have going on... I want to see the NET of all trades" — the
per-row summary was buried in each enemy node and you had to
mentally sum them. The flow chips give it at a glance. (2) "I
should see one panel with everyone offering to trade me, so I can
rank the best deal" — pop-up modals showed one offer at a time
with no way to compare. The ranked list shows them all together,
sorted by score, with the best deals highlighted green. The AI
desperation change adds variance — without it every offer was
identical and "rank the best deal" had nothing to rank.
