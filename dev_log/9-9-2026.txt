Build 41 (metro2): SKYLINE REALISM + LANDMARKS (player: "too many
tall buildings all over... mid sized buildings more frequent, building
size variation, add a stadium or an airport"). Height distribution
reworked to real-city fabric: ~66% low-rise (houses + rowhouse slabs,
h 2-5), 24% mid-rise, 10% towers -- and towers only scale up where the
downtown core boost and station demand agree, so the spine reads and
the edges calm down. Denser block counts, wider scatter, more
footprint variety. TWO LANDMARKS, both sited by a clearance-search
script against rail/stations/parks and reserved from building/tree
placement: THE MERIDIAN BOWL (west bank, 44/336) -- elliptical bowl,
green pitch, four floodlight masts that burn at night; BAYSIDE FIELD
(east shore, 484/450) -- 64-unit runway with centerline dashes,
apron, terminal, control tower with a blinking red beacon at night,
two parked planes. Day + night screenshots reviewed before deploy;
the stadium's night floodlights are the new beauty shot.

Build 42 (metro2): M3a -- THE UPGRADE ECONOMY, PORTED (player: "What
happened to all our other upgrades... We lost a lot of functionality").
The whole v1 economy is back, formula for formula: per-line upgrades
(speed/cars/access/trainsets with v1 cost curves), the SIX station
works with per-station buying on the station card AND the per-line
tier-even bulk planner with player-set priority (raise with the ▲),
all EIGHT network upgrades, and the full income/demand model
(currentFare, effectiveDwell + platform works, demandMultAt
compounding across serving lines, incomePerRiderAt, stationCapAt,
offlineEfficiencyNow, rail-yard train discounts). Save format v2s2
(additive; v2s1 loads clean). 17 vitest tests green including v1's
own measured must-show-up bounds for every upgrade -- the port passes
v1's harness thresholds unchanged. UI: LINE DESK rows now open a full
line sheet (trains, upgrades, works planner), NETWORK desk button,
station card sells all six works and shows demand x / $ per rider.
Map: track ribbons widened (1.25->1.7) with a daytime emissive
baseline so route colors stay saturated; ghost routes 0.14->0.3 so
the unbuilt network reads. Headless play test drove: buy speed, buy a
17-station food tier, buy signals, verify the 5s autosave payload,
true reload -> everything persisted. OPS (rush desk + commissions) and
GOALS are M3b, next build.

Build 43 (metro2): M3b -- OPS + GOALS + THE 10x (player: "Fix the gap
and bring back my 10x"). The console is the full v1 four-tab desk
again: LINES · OPS · GOALS · NETWORK. OPS = the rush timetable (next
4 cycles off the deterministic clock) + the commission desk with all
FIVE contract types (haul / turnback run / hub service / clean sweep
/ rush contract), v1 quotas, per-type clocks, uniform rewards, and
per-type offer/progress copy. GOALS = the 22-rung New Meridian ladder
verbatim (incl. the mastery rungs: contracts, works, upgrade levels,
rush earnings), commendations compounding income exactly as v1, a
goal status strip under the header (tap -> board), commendation and
commission toasts, an OPS attention dot while an offer waits. The
⏩10x fast-forward is back as a header toggle (same fixed-step 0.05s
sub-ticks -- exact physics, compressed). Save v2s3 adds commissions,
goalsDoneByCity, rushEarnings; goalMult is RECOMPUTED from the ladder
on load. 26 vitest tests green -- the b36 v1 suite ported whole
(rotation, turnback win, hub bare-fails/invested-wins, sweep instant
win, rush-window-only progress, hopeless expiry, ladder completion
with exact income product). Headless play test accepted a TURNBACK
RUN (0/6 · 2:00), read income x3.30 off the goals head (5 rungs =
1.25^3 x 1.3^2, the 5th auto-completing from the seeded riders), and
toggled the 10x. Remaining for full parity: Angel Bay + moveOn (M3c),
v1-save migration (M4).

Build 44 (metro2): iOS ZOOM-TRAP FIX (player: stations near another
line hard to click; double-tapping them "makes the whole screen zoom
in (buttons too) and I can't fix it without refresh"). Root cause:
iOS Safari IGNORES user-scalable=no for double-tap and pinch PAGE
zoom -- rapid re-taps on a fiddly station triggered browser zoom of
the whole page, and the same flag then blocks pinching back out.
Fix, three layers: (1) touch-action: manipulation on the page
(the modern, honored way to disable double-tap zoom; the scrolling
panel gets pan-y + overscroll-behavior: contain, the canvas keeps
none), (2) gesturestart/change/end + dblclick preventDefault, (3) a
320ms double-tap suppressor scoped to the CANVAS ONLY so buy buttons
keep fast repeated taps (canvas input is pointer-event driven, so no
click is lost). Also the trigger itself: station pick tolerance
raised (min 4->5.5 world units, zoom scale 0.02->0.028) so stops
sitting against a neighboring line's ribbon are comfortably
tappable. 26 tests green; headless regression re-ran the buy +
persistence flow with the guards active.