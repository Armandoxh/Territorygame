.dart_tool/
build/
.flutter-plugins
.flutter-plugins-dependencies
# Platform scaffolding is generated in CI by `flutter create` — only lib/,
# test/, web tweaks, and pubspec live in git.
android/
ios/
linux/
macos/
windows/
web/
